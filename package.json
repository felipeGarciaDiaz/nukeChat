{
    "name": "test",
    "version": "1.0.0",
    "scripts": {
        "start": "npm run build-client && node server",
        "start-local": "npm run build-client && PORT=4040 node server",
        "build-client": "cd client && npm run build && cd .."
    },
    "dependencies": {
        "express": "^4.17.1"
    },
    "devDependencies": {
        "socket.io": "^2.4.0"
    }
}
